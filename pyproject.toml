[project]
name = "aurora"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.9",
]

[tool.pytest.ini_options]
testpaths = ["aurora/tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
